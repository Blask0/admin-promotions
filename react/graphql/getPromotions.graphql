query GetPromotions($name: String, $effect: String) {
  getPromotions(name: $name, effect: $effect) {
    cacheId
    id
    name
    beginDate
    endDate
    scope {
      allCatalog
      skus
      products
      collections
      categories
      brands
    }
    isActive
    effectType
  }
}
