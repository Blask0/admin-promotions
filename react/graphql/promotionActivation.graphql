mutation PromotionActivation($promotion: SimplifiedPromotionInput!) {
  promotionActivation(promotion: $promotion) {
    cacheId
    id
    name
    beginDate
    endDate
    isActive
    effectType
  }
}
